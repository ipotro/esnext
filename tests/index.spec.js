import { expect } from 'chai';

describe('Learning ES6 with TDD', () => {
  it('should be fun', () => {
    const FUN = true;

    expect(FUN).to.be.true; // eslint-disable-line no-unused-expressions
  });
});
